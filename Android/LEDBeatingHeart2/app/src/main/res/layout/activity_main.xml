<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="#ffedcc">
 
    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:orientation="vertical">
 
 
		<TextView
	        android:layout_width="wrap_content"
	        android:layout_height="wrap_content"
	        android:layout_marginTop="20dp"
	        android:text="IP Address:"
	        android:id="@+id/textView" />	 
	    <EditText
	        android:layout_width="match_parent"
	        android:layout_height="wrap_content"
	        android:hint="e.g. 192.168.0.10"
	        android:id="@+id/editTextIPAddress" />
	 
	    <TextView
	        android:layout_width="wrap_content"
	        android:layout_height="wrap_content"
	        android:text="Port Number:"
	        android:id="@+id/textView2" />	 
	    <EditText
	        android:layout_width="match_parent"
	        android:layout_height="wrap_content"
	        android:inputType="number"
	        android:ems="10"
	        android:hint="e.g. 80"
	        android:id="@+id/editTextPortNumber" />


		<Button
			android:id="@+id/buttonHeartBeat"
			android:layout_width="match_parent"
			android:layout_height="wrap_content"
			android:alpha="1.0"
			android:background="#000000"
			android:drawableTop="@drawable/ic_action_heart"
			android:text="@string/button_select_heart_beat"
			android:textColor="#757575"
			android:textSize="16sp" />

		<Button
			android:id="@+id/buttonHeartFade"
			android:layout_width="match_parent"
			android:layout_height="wrap_content"
			android:alpha="1.0"
			android:background="#000000"
			android:drawableTop="@drawable/ic_action_heart"
			android:text="@string/button_select_heart_fade"
			android:textColor="#757575"
			android:textSize="16sp" />
    
    </LinearLayout>
 
</ScrollView>

